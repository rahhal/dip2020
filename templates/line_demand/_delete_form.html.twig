<form method="post" action="{{ path('line_demand_delete', {'id': line_demand.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ line_demand.id) }}">
    <button class="btn">Delete</button>
</form>
