
{% extends 'base.html.twig' %}

{% block title %} {{ app.user.email|upper }} {% endblock %}
{% block body_class %} class="nav-md" {% endblock %}

{% block body %}
    <div class="container body">
        <div class="main_container">
            <div class="col-md-3 left_col hidden-print">
                {% include('layouts/sidebar.html.twig') %}
            </div>
            {% include('layouts/navbar.html.twig') %}
            <!-- page content -->
            <div class="right_col" role="main">
                <div class="page-title">
                    <div class="title_left">
                        <h3>{{ "Details of nb meal"|trans }}</h3>
                    </div>
                </div>

                <div class="clearfix"></div>
                <div class="row">
                    <div class="col-md-12 col-sm-12 col-xs-12">
                        <div class="x_panel">
                            <div class="x_content">
                                <table class="table">
                                    <tbody>
                                    <tr>
                                        <th>{{ "Day"|trans }}</th>
                                        <td>{{ nbMeal.date |date('Y-m-d')}}</td>                                    </tr>
                                    <tr>
                                        <th>{{ "Number of resident studiants"|trans }}</th>
                                        <td>{{ nbMeal.stdResident }}</td>
                                    </tr>
                                    <tr>
                                        <th>{{ "Number of semi-resident studiants"|trans }}</th>
                                        <td>{{ nbMeal.stdSemiResident }}</td>
                                    </tr>
                                    <tr>
                                        <th>{{ "Number of granted studiants"|trans }}</th>
                                        <td>{{ nbMeal.stdGranted }}</td>
                                    </tr>
                                    <tr>
                                        <th>{{ "Number of professors"|trans }}</th>
                                        <td>{{ nbMeal.professor }}</td>
                                    </tr>
                                    <tr>
                                        <th>{{ "Number of curators"|trans }}</th>
                                        <td>{{ nbMeal.curators }}</td>
                                    </tr>
                                    <tr>
                                        <th>{{ "Number of employees"|trans }}</th>
                                        <td>{{ nbMeal.employee }}</td>
                                    </tr>

                                    </tbody>
                                </table>

                                 {#<div class="form-group col-lg-12">
                                     <div class="ln_solid"></div>
                                     <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                                         <div class="col-md-2 col-sm-2 col-xs-2 col-md-offset-3">
                                             {{ include('nb_meal/_delete_form.html.twig') }}
                                         </div>
                                         <a href="{{ path('nb_meal_edit', {'id': nbMeal.id}) }}" class="btn btn-primary"> <i class="fa fa-pencil" style="margin-left: 2rem"></i>{% trans %} Update {% endtrans %}</a>
                                            <a href="{{ path('nb_meal_index') }}" class="btn btn-default" data-toggle="popover"><i class="fa fa-backward" style="margin-left: 2rem"></i>{% trans %} back to list {% endtrans %}</a>
                                     </div>
                                 </div>#}
                                <div class="form-group">
                                    <div class="col-md-2 col-sm-2 col-xs-2 col-md-offset-3">
                                        {{ include('nb_meal/_delete_form.html.twig') }}
                                    </div>
                                    <a href="{{ path('nb_meal_edit', {'id': nbMeal.id}) }}" class="btn btn-primary"><i class="fa fa-edit" style="margin-left: 2rem"></i>{% trans %} Update {% endtrans %}</a>
                                    <a href="{{ path('nb_meal_index') }}" class="btn btn-default" data-toggle="popover"><i class="fa fa-backward" style="margin-left: 2rem"></i>{% trans %} back to list {% endtrans %}</a>
                                </div>
                                {#<div class="form-group col-lg-12">
                                    <div class="ln_solid"></div>
                                    <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                                        <form method="post" action="{{ path('nb_meal_delete', {'id': nbMeal.id}) }}" onsubmit="return confirm('هل انت متأكد انك ترغب في حذف هذا العنصر؟');">
                                            <input type="hidden" name="_method" value="DELETE">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nbMeal.id) }}">
                                            <button class="btn btn-danger"><i class="fa fa-trash " style="margin-left: 2rem"></i>{{  "Delete" | trans }}</button>
                                            <a href="{{ path('nb_meal_edit', {'id': nbMeal.id}) }}" class="btn btn-primary"><i class="fa fa-pencil" style="margin-left: 2rem"></i>{% trans %} Update {% endtrans %}</a>
                                            <a href="{{ path('nb_meal_index') }}" class="btn btn-default" data-toggle="popover"><i class="fa fa-backward" style="margin-left: 2rem"></i>{% trans %} back to list {% endtrans %}</a>

                                        </form>

                                    </div>
                                </div>#}
                            </div>
                        </div>
                    </div>

                </div>
            </div>
            {% include('layouts/footer.html.twig') %}
        </div>
    </div>
{% endblock %}

