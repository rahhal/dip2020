{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-primary"><i class="fa fa-save" style="margin-left: 2rem"></i>{{ button_label|default('Save')|trans }}</button>{{ form_end(form) }}
