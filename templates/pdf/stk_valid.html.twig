<!DOCTYPE html>
<html dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>صلوحية المواد</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }
        td, th {
            border: 1px solid #000;
            text-align: left;
            padding: 8px;
        }
    </style>
</head>
<body style="font-family:Arial">
<h4 style="text-align: right">  الجمهورية التونسية </h4>

{% for vb in institution %}

    {% if loop.first %}

        <h4 style="text-align: right">   {{ vb.ministry }} </h4>
        <h4 style="text-align: right">    {{ vb.office }}</h4>
        <h4 style="text-align: right">    {{ vb.name }}</h4>
    {% endif %}
{% endfor %}
<h2 style="text-align: center">تواريخ صلوحية المواد</h2>
        <br>
        <br>
        <br>
        <br>
<br>
<br>
<table>
    <thead>
    <tr>
        <th style="text-align: center" >{{ "Name of article"|trans }}</th>
        <th style="text-align: center">{{ "Prod date"|trans }}</th>
        <th style="text-align: center">{{ "Valid date"|trans }}</th>
    </tr>
    </thead>

    <tbody>
    {% for lineStock in lineStocks %}
        <tr>
            <td style="text-align: center">{{ lineStock.linePurchase.article }}</td>
            <td style="text-align: center">{{ lineStock.prodDate? lineStock.prodDate|date('Y/m/d') : '' }}</td>
            <td style="text-align: center">{{ lineStock.validDate? lineStock.validDate|date('Y/m/d') : '' }}</td>
        </tr>
    {% endfor %}

    </tbody>
</table>

<br>
<br>
<h4 style="text-align: center">حافظ المغازة</h4>
</body>
</html>