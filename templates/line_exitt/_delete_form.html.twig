<form method="post" action="{{ path('line_exitt_delete', {'id': line_exitt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ line_exitt.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash " style="margin-left: 2rem"></i>{{ "Delete"|trans }}</button>
</form>
