

{% extends 'base.html.twig' %}

{% block title %} {{ app.user.email|upper }} {% endblock %}
{% block body_class %} class="nav-md" {% endblock %}

{% block body %}
    <div class="container body">
        <div class="main_container">
            <div class="col-md-3 left_col hidden-print">
                {% include('layouts/sidebar.html.twig') %}
            </div>
            {% include('layouts/navbar.html.twig') %}
            <!-- page content -->
            <div class="right_col" role="main">
                <div class="page-title">
                    <div class="title_left">
                        <h3>{{ " Details of admin" |trans }}</h3>
                    </div>
                </div>
                <div class="clearfix"></div>
                <div class="row">
                    <div class="col-md-12 col-sm-12 col-xs-12">
                        <div class="x_panel">
                            <div class="x_content">

                                <table class="table">
                                    <tbody>
                                    <tr>
                                        <th>{{ "Fullname" |trans }}</th>
                                        <td>{{ admin.fullname }}</td>
                                    </tr>
                                  {#  <tr>
                                        <th>{{ "Phone" |trans }}</th>
                                        <td>{{ admin.phone }}</td>
                                    </tr>#}
                                    <tr>
                                        <th>{{ "Email" |trans }}</th>
                                        <td>{{ admin.email }}</td>
                                    </tr>
                                    </tbody>
                                </table>

                                <div class="form-group">
                                    <div class="col-md-2 col-sm-2 col-xs-2 col-md-offset-3">
                                        {{ include('user/_delete_form1.html.twig') }}
                                    </div>
                                    <a href="{{ path('admin_edit', {'id': admin.id}) }}" class="btn btn-primary"><i class="fa fa-edit" style="margin-left: 2rem"></i>{% trans %} Update {% endtrans %}</a>
                                    <a href="{{ path('admin_list') }}" class="btn btn-default" data-toggle="popover"><i class="fa fa-backward" style="margin-left: 2rem"></i>{% trans %} back to list {% endtrans %}</a>
                                </div>

                            </div>
                        </div>
                    </div>

                </div>
            </div>
            {% include('layouts/footer.html.twig') %}
        </div>
    </div>

{% endblock %}
