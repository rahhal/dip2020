{% extends 'base.html.twig' %}

{% block title %}LinePurchase{% endblock %}

{% block body %}
    <h1>LinePurchase</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>QuantityRequired</th>
            <td>{{ line_purchase.quantityRequired }}</td>
        </tr>
            <tr>
                <th>QuantityDelivred</th>
                <td>{{ line_purchase.quantityDelivred }}</td>
            </tr>
            <tr>
                <th>total price</th>
                <td>{{ line_purchase.totalPrice }}</td>
            </tr>
            <tr>
                <th>Unit_price</th>
                <td>{{ line_purchase.unitPrice }}</td>
            </tr>
            <tr>
                <th>Tax</th>
                <td>{{ line_purchase.tax }}</td>
            </tr>
            <tr>
                <th>تاريخ الانتهاء</th>
                <td>{{ line_purchase.validation ? exitt.validation|date('Y-m-d ') : '' }}</td>
            </tr>
            <tr>
                <th>تاريخ الصنع</th>
                <td>{{ line_purchase.production ? exitt.production|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('line_purchase_index') }}">back to list</a>

    <a href="{{ path('line_purchase_edit', {'id': line_purchase.id}) }}">edit</a>

    {{ include('line_purchase/_delete_form.html.twig') }}
{% endblock %}
